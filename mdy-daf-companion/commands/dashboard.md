---
description: Open the local MDY Daf Companion dashboard.
---

Run `mdy-daf open-dashboard`. If `mdy-daf` is not available on PATH, run `node "${CLAUDE_PLUGIN_ROOT}/dist/src/cli.js" open-dashboard`. Report the local dashboard URL and explain that it is token-protected and served by the local daemon.
