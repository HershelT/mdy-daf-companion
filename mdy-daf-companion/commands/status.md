---
description: Show current MDY Daf Companion playback and stats status.
---

Run the local `mdy-daf status` command and summarize the current daf, playback progress, coding time, and any setup issue. If `mdy-daf` is not available on PATH, run `node "${CLAUDE_PLUGIN_ROOT}/dist/src/cli.js" status`.
