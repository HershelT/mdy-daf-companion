---
description: Start or resume the current MDY Daf Yomi shiur.
---

Run `mdy-daf open-player` for an explicit play/open request, or `mdy-daf resume` if the player is already open. If `mdy-daf` is not available on PATH, use `node "${CLAUDE_PLUGIN_ROOT}/dist/src/cli.js" open-player`.
